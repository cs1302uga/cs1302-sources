<project xmlns="http://maven.apache.org/DECORATION/1.8.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/DECORATION/1.8.0 http://maven.apache.org/xsd/decoration-1.8.0.xsd"
         name="CSCI 1302 @ UGA CS">

  <skin>
    <groupId>io.github.cs1302uga</groupId>
    <artifactId>cs1302-site-skin</artifactId>
    <version>0.1.80</version>
  </skin>

  <custom>
    <cs1302>
    </cs1302>
  </custom>

  <body>

    <breadcrumbs>
      <item name="${this.groupId}" href="${this.organization.url}"/>
      <item name="${this.name}" href="${this.url}"/>
    </breadcrumbs>

    <menu>
      <item name="About" href="${this.url}" />
    </menu>

    <menu ref="reports" />
    <menu ref="modules" />
    <menu ref="parent" />

    <footer>
      <![CDATA[

        <p>
          &copy; ${this.inceptionYear} <a href="${this.organization.url}">${this.organization.name}</a>.
        </p>
        <p class="text-muted">
          The content and opinions expressed on this Web page do not necessarily reflect the views of nor are they endorsed by the University of Georgia or the University System of Georgia.
        </p>

      ]]>
    </footer>

  </body>

</project>
