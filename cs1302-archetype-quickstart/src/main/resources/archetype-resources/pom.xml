<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>
  <version>${version}</version>
  <packaging>jar</packaging>

  <name>${artifactId}</name>
  <url>https://cs.uga.edu</url>

  <properties>
    <cs1302.java.version>11</cs1302.java.version>
    <cs1302.encoding>UTF-8</cs1302.encoding>
    <maven.compiler.source>${cs1302.java.version}</maven.compiler.source>
    <maven.compiler.target>${cs1302.java.version}</maven.compiler.target>
    <project.build.sourceEncoding>${cs1302.encoding}</project.build.sourceEncoding>
    <project.reporting.outputEncoding>${cs1302.encoding}</project.reporting.outputEncoding>
  </properties>

  <dependencies>
    <!-- DEPENDENCIES -->
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <compilerArgs>
            <arg>-Xlint:unchecked</arg>
          </compilerArgs>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
